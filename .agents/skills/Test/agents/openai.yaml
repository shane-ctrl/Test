interface:
  display_name: "Test"
  short_description: "Repo-specific patterns and workflows for Test"
  default_prompt: "Use the Test repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true